# Environment Variables for Decision Trace
# Copy this file to .env and fill in your values
# NEVER commit .env files with real secrets!

# ============================================
# DEMO MODE (Default for Hackathon)
# ============================================
# Set to 'true' to enable demo mode (no API key required)
# Demo mode auto-enables if GEMINI_API_KEY is not set
DEMO_MODE=true

# ============================================
# GEMINI API CONFIGURATION
# ============================================
# Google Gemini API Key (optional - demo mode works without it)
# Get your key from: https://makersuite.google.com/app/apikey
# Leave blank to use demo mode (recommended for hackathon)
GEMINI_API_KEY=

# Gemini Test Mode (for testing only)
# Options: 'mock' | 'replay' | 'live'
# Default: 'mock' in test environment, 'live' in production
GEMINI_TEST_MODE=

# ============================================
# DATABASE CONFIGURATION
# ============================================
# SQLite (for local development and tests)
# Example: file:/absolute/path/to/db.sqlite
DATABASE_URL=file:./tmp/demo.db

# PostgreSQL (for production)
# Example: postgresql://user:password@host:5432/database
# DATABASE_URL=postgresql://user:password@localhost:5432/decisiontrace

# Prisma Schema Target
# Options: 'sqlite' | 'postgres'
# Default: 'sqlite' for tests, 'postgres' for production
PRISMA_SCHEMA_TARGET=sqlite

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Node Environment
# Options: 'development' | 'test' | 'production'
NODE_ENV=development

# ============================================
# FREE TIER MODE (Optional)
# ============================================
# Enable free tier limits (rate limiting, file size limits)
# Set to 'true' to enable strict free tier constraints
FREE_MODE=false

# Allow PDF files in free mode (default: false)
FREE_PDF_ALLOWED=false

# ============================================
# CI/CD CONFIGURATION
# ============================================
# CI environment flag (automatically set by CI systems)
# CI=true

# ============================================
# VERCEL DEPLOYMENT (Production)
# ============================================
# For Vercel deployment, set these in Vercel Dashboard:
# - DEMO_MODE=true (for hackathon demo)
# - GEMINI_API_KEY=your-key (optional, only if not using demo mode)
# - DATABASE_URL (automatically set if using Vercel Postgres)

# ============================================
# NOTES
# ============================================
# 1. Demo Mode is DEFAULT for hackathon deployments
#    - Set DEMO_MODE=true OR omit GEMINI_API_KEY
#    - No API key required
#    - Works without database persistence
#
# 2. For Production with Live Gemini:
#    - Set GEMINI_API_KEY=your-actual-key
#    - Set DATABASE_URL to your Postgres connection string
#    - Demo mode will be disabled automatically
#
# 3. NEVER commit this file with real secrets!
#    - This .env.example is safe to commit
#    - Your actual .env file should be in .gitignore

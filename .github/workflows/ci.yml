name: CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test:
    name: Test Suite
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [20.x]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma clients
        run: npm run prisma:generate

      - name: Type check
        run: npm run typecheck
        continue-on-error: false

      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Run unit tests
        env:
          NODE_ENV: test
          GEMINI_API_KEY: mock-api-key-for-testing
          GEMINI_TEST_MODE: mock
        run: npm run test:unit

      - name: Generate SQLite Prisma client for integration tests
        env:
          PRISMA_SCHEMA_TARGET: sqlite
        run: npm run prisma:generate:sqlite

      - name: Run integration tests
        env:
          NODE_ENV: test
          GEMINI_API_KEY: mock-api-key-for-testing
          GEMINI_TEST_MODE: mock
          TEST_DATABASE_URL: file:/tmp/test-decision-trace.db
          DATABASE_URL: file:/tmp/test-decision-trace.db
          PRISMA_SCHEMA_TARGET: sqlite
        run: npm run test:integration

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Setup E2E test database
        run: mkdir -p tmp

      - name: Setup E2E database schema
        env:
          DATABASE_URL: file:./tmp/e2e.db
          PRISMA_SCHEMA_TARGET: sqlite
        run: |
          npx prisma generate --schema=prisma/schema.prisma
          npx prisma db push --schema=prisma/schema.prisma --accept-data-loss
          npx tsx prisma/seed.ts

      - name: Build for E2E tests
        env:
          DATABASE_URL: file:./tmp/e2e.db
          PRISMA_SCHEMA_TARGET: sqlite
          GEMINI_TEST_MODE: mock
          FREE_MODE: true
        run: npx next build

      - name: Run Playwright E2E tests
        env:
          NODE_ENV: test
          DATABASE_URL: file:./tmp/e2e.db
          PRISMA_SCHEMA_TARGET: sqlite
          GEMINI_TEST_MODE: mock
          FREE_MODE: true
          CI: true
          BASE_URL: http://localhost:3000
        run: npm run test:e2e:smoke

      - name: Upload Playwright traces
        uses: actions/upload-artifact@v4
        if: failure()
        continue-on-error: true
        with:
          name: playwright-traces-${{ matrix.node-version }}-${{ github.run_number }}
          path: |
            test-results/
            playwright-report/
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload test coverage
        uses: actions/upload-artifact@v4
        if: always()
        continue-on-error: true
        with:
          name: coverage-${{ matrix.node-version }}
          path: coverage/
          retention-days: 7
          if-no-files-found: ignore

  e2e-only:
    name: E2E Tests (Full Browser Matrix)
    runs-on: ubuntu-latest
    needs: test
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Setup E2E test database
        run: mkdir -p tmp

      - name: Setup E2E database schema
        env:
          DATABASE_URL: file:./tmp/e2e.db
          PRISMA_SCHEMA_TARGET: sqlite
        run: |
          npx prisma generate --schema=prisma/schema.prisma
          npx prisma db push --schema=prisma/schema.prisma --accept-data-loss
          npx tsx prisma/seed.ts

      - name: Build for E2E tests
        env:
          DATABASE_URL: file:./tmp/e2e.db
          PRISMA_SCHEMA_TARGET: sqlite
          GEMINI_TEST_MODE: mock
          FREE_MODE: true
        run: npx next build

      - name: Run Playwright E2E tests
        env:
          NODE_ENV: test
          DATABASE_URL: file:./tmp/e2e.db
          PRISMA_SCHEMA_TARGET: sqlite
          GEMINI_TEST_MODE: mock
          FREE_MODE: true
          CI: true
          BASE_URL: http://localhost:3000
        run: npm run test:e2e:smoke

      - name: Upload Playwright traces
        uses: actions/upload-artifact@v4
        if: failure()
        continue-on-error: true
        with:
          name: playwright-traces-full-${{ github.run_number }}
          path: |
            test-results/
            playwright-report/
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        continue-on-error: true
        with:
          name: playwright-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 7
          if-no-files-found: ignore

  postgres-migration:
    name: Postgres Migration Tests
    runs-on: ubuntu-latest
    needs: test
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Postgres Prisma client
        env:
          PRISMA_SCHEMA_TARGET: postgres
        run: npm run prisma:generate:pg

      - name: Run Prisma migrations (Postgres)
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/test_db
          PRISMA_SCHEMA_TARGET: postgres
        run: |
          npm run prisma:migrate:pg || echo "Migrations may already be applied"

      - name: Run Postgres migration tests
        env:
          NODE_ENV: test
          GEMINI_API_KEY: mock-api-key-for-testing
          GEMINI_TEST_MODE: mock
          TEST_POSTGRES_URL: postgresql://test:test@localhost:5432/test_db
          DATABASE_URL: postgresql://test:test@localhost:5432/test_db
          PRISMA_SCHEMA_TARGET: postgres
        run: npm run test:integration -- tests/integration/migration-safety.test.ts

